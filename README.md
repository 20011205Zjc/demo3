# React 数据大屏

基于 Vite、React 和 TypeScript 的数据大屏示例项目，包含 KPI 指标卡、城市排行、渠道占比、趋势折线、节点态势和实时事件流。

## 开发

```bash
npm install
npm run dev
```

默认访问地址：

```text
http://localhost:5173/
```

## 构建

```bash
npm run build
```

业务数据集中在 `src/data.ts`，页面结构在 `src/App.tsx`，视觉样式在 `src/styles.css`。
